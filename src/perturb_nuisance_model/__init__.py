"""Archived empirical scoring modules retained for provenance.

The repository exposes no package-level convenience API; active scripts import
the hash-bound submodules they require.
"""

__all__: tuple[str, ...] = ()
