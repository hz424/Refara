diff --git a/scgen/_scgenvae.py b/scgen/_scgenvae.py
--- a/scgen/_scgenvae.py
+++ b/scgen/_scgenvae.py
@@ -101,7 +101,9 @@ class SCGENVAE(BaseModuleClass, VAEMixin):
         """
         qz_m, qz_v, z = self.z_encoder(x)
 
-        outputs = dict(z=z, qz_m=qz_m, qz_v=qz_v)
+        # scvi-tools >=1.3 VAEMixin expects qzm/qzv aliases, while scGen
+        # internally uses qz_m/qz_v. Keep both names for compatibility.
+        outputs = dict(z=z, qz_m=qz_m, qz_v=qz_v, qzm=qz_m, qzv=qz_v)
         return outputs
 
     @auto_move_data
