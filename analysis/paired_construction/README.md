# Paired construction-change source checks

`source_tables.py` checks the saved 56-comparison family and all 448 root contrasts against their fixed hashes.

For complete numerical reconstruction from the accompanying Source Data, use `scripts/build_gse162632_paired_construction_changes_public_v1.py --check --source-data-root /path/to/05_SOURCE_DATA` from the repository root. The empirical family remains post hoc and distinct from the original 84-comparison family.
