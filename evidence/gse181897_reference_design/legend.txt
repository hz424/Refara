Reference separation preserves the selected model in the external study.

a, Model ranks for six allocations of a total of 16 control cells, based on equal-donor selection loss in 16 GSE181897 donors. Separate allocations list input + observation-reference cells; prediction centring uses the input cells. Circles mark the selected family. State-model scores average three separately scored fits; direct models use one fit. b, Paired independent perturbation-effect losses for all 46 assessment donors (777 donor–context–condition tasks). Each line joins the same donor under the shared-selected and separate-selected model. Both designs selected PCA, producing identical losses. Scoring uses 1,968 genes with fixed training scales; each donor's eligible contexts and three conditions have equal weight. c, Mean practical margin, 0.95 × shared loss − separate loss, with two-sided 95% paired-donor t and studentized-bootstrap intervals (19,999 draws). Both lower bounds must exceed zero to support at least 5% improvement. Negative intervals here indicate failure to attain that threshold; the zero-effect contrast is exactly zero because the selected model is identical. Deployment inputs and measured targets are common across models, with eight input controls held apart from target controls. Inference conditions on the observed acquisition pools, fixed adapters and selection split. Secondary analyses and complete model losses accompany the numerical replay.
